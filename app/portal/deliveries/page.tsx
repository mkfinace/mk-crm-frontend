'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link';
export default function DeliveriesPage(){const [items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''); useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_URL||'https://mk-crm-backend.onrender.com'}/portal/my/deliveries`,{headers:{Authorization:`Bearer ${localStorage.getItem('mk_portal_token')||''}`}}).then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.message||'Unable to load deliveries');return d}).then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div className="max-w-[1000px] mx-auto"><div className="mb-7"><p className="text-[11px] font-bold tracking-[3px] uppercase text-[#146BFF]">Delivery</p><h1 className="text-[28px] font-extrabold tracking-tight mt-1">My Vehicle Delivery</h1><p className="text-[13px] text-[#8894A5] mt-1">Track delivery schedule and status for your booked vehicle.</p></div>{loading?<div className="bg-white border rounded-2xl p-6">Loading delivery details…</div>:error?<div className="bg-red-50 border border-red-200 text-red-700 rounded-2xl p-5 text-sm">{error}</div>:items.length===0?<div className="bg-white border rounded-2xl p-7 text-sm text-[#68758A]">Delivery is not scheduled yet.</div>:<div className="space-y-3">{items.map(d=><Link key={d.id} href={`/portal/leads/${d.leadId}`} className="block bg-white border border-[#E3E8EF] rounded-2xl p-5"><div className="flex justify-between gap-4"><div><h2 className="font-extrabold">{d.lead?.brand?.name} {d.lead?.model?.name}</h2><p className="text-xs text-[#8894A5] mt-1">{d.lead?.variant?.name||'Variant not specified'} · {d.lead?.leadCode}</p></div><span className="px-3 py-1 rounded-full bg-[#F0F6FF] text-[#146BFF] text-xs font-bold">{d.status||'SCHEDULED'}</span></div><div className="grid grid-cols-2 sm:grid-cols-3 gap-4 mt-5 text-sm"><div><p className="text-[10px] uppercase text-[#94A0AF]">Scheduled</p><p className="font-bold mt-1">{d.scheduledAt?new Date(d.scheduledAt).toLocaleString('en-IN'):'—'}</p></div><div><p className="text-[10px] uppercase text-[#94A0AF]">Delivered</p><p className="font-bold mt-1">{d.deliveredAt?new Date(d.deliveredAt).toLocaleDateString('en-IN'):'Pending'}</p></div><div><p className="text-[10px] uppercase text-[#94A0AF]">Vehicle</p><p className="font-bold mt-1">{d.lead?.variant?.name||d.lead?.model?.name||'—'}</p></div></div></Link>)}</div>}</div>}
