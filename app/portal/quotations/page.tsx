'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link';
export default function QuotationsPage(){const [items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_URL||'https://mk-crm-backend.onrender.com'}/portal/my/quotations`,{headers:{Authorization:`Bearer ${localStorage.getItem('mk_portal_token')||''}`}}).then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.message||'Unable to load quotations');return Array.isArray(d)?d:d.items||[]}).then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div className="max-w-[1000px] mx-auto"><div className="mb-7"><p className="text-[11px] font-bold tracking-[3px] uppercase text-[#146BFF]">Quotation</p><h1 className="text-[28px] font-extrabold tracking-tight mt-1">My Quotations</h1><p className="text-[13px] text-[#8894A5] mt-1">Vehicle-wise quotations shared by MK Finance.</p></div>{loading?<div className="bg-white border rounded-2xl p-6">Loading quotations…</div>:error?<div className="bg-red-50 border border-red-200 text-red-700 rounded-2xl p-5 text-sm">{error}</div>:items.length===0?<div className="bg-white border rounded-2xl p-7 text-sm text-[#68758A]">No quotation has been shared yet.</div>:<div className="space-y-3">{items.map(q=><div key={q.id} className="bg-white border border-[#E3E8EF] rounded-2xl p-5 hover:border-[#146BFF]/40"><div className="flex justify-between gap-4"><div><h2 className="font-extrabold">{q.lead?.brand?.name} {q.lead?.model?.name}</h2><p className="text-xs text-[#8894A5] mt-1">{q.lead?.variant?.name||'Variant not specified'} · {q.lead?.leadCode}</p></div><span className="text-xs font-bold px-3 py-1 rounded-full bg-[#F0F6FF] text-[#146BFF]">v{q.version}</span></div><div className="grid grid-cols-2 sm:grid-cols-4 gap-4 mt-5 text-sm"><div><p className="text-[10px] uppercase text-[#94A0AF]">Ex-showroom</p><p className="font-bold mt-1">₹{Number(q.exShowroomPrice||q.price||0).toLocaleString('en-IN')}</p></div><div><p className="text-[10px] uppercase text-[#94A0AF]">On-road</p><p className="font-bold mt-1">₹{Number(q.onRoadPrice||0).toLocaleString('en-IN')}</p></div><div><p className="text-[10px] uppercase text-[#94A0AF]">Valid Till</p><p className="font-bold mt-1">{q.validTill?new Date(q.validTill).toLocaleDateString('en-IN'):'—'}</p></div><div><p className="text-[10px] uppercase text-[#94A0AF]">Discount</p><p className="font-bold mt-1">₹{Number(q.discount||0).toLocaleString('en-IN')}</p></div></div><div className="flex flex-wrap gap-2 mt-4 pt-4 border-t border-[#EEF1F5]"><Link href={`/portal/quotations/${q.id}`} className="rounded-lg bg-[#146BFF] text-white px-4 py-2 text-[11.5px] font-semibold">View Full Quotation →</Link>{q.leadId&&<Link href={`/portal/leads/${q.leadId}`} className="rounded-lg border border-[#E3E8EF] px-4 py-2 text-[11.5px] font-semibold">View Vehicle & Enquiry</Link>}</div></div>)}</div>}</div>}
